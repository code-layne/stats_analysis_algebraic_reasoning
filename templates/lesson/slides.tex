%! @@TITLE@@ — Unit @@UNITINT@@, Lesson @@LESSONID@@ — Slides (Beamer)
% PILOT SHAPE (2026-09-06). GRADUAL RELEASE deck (~14 frames): title → targets → warm-up → hook
% (dark, UNRESOLVED) → I DO divider (dark) → two or three frames per notes section (crux frame
% flagged \sectionlabel[redacc]{}) → Guided Practice (a LIVE surface, un-answered) → spoken
% debrief with the cold check → You do: START THE HOMEWORK → close (dark). No practice launch,
% no exit ticket. There is no spoiler rule. Mirror unit01/lesson02/slides.
\documentclass[aspectratio=169, 11pt]{beamer}
\usepackage{@@PREFIX@@-beamer}   % provides \ceruleanheader{} and \sectionlabel[color]{}

\newcommand{\redink}[1]{{\color{redacc}\bfseries #1}}

\begin{document}

% TITLE SLIDE (CourseName is not defined in beamer, so the name is literal)
{
\setbeamercolor{background canvas}{bg=cerulean}
\begin{frame}[plain]
  \vspace{2.8cm}\hspace{0.55cm}%
  \begin{minipage}{0.9\textwidth}
    {\color{goldacc}\bfseries\small LESSON @@LESSONID@@}\\[0.5cm]
    {\color{white}\bfseries\LARGE @@TITLE@@}\\[1.2cm]
    {\color{frostmid}\small @@COURSENAME@@~$\cdot$~Unit @@UNITINT@@}
  \end{minipage}
\end{frame}
}

% ── LEARNING TARGETS — worded as on the cover ────────────────────────────────
\begin{frame}[t, plain]
  \ceruleanheader{Today's targets}
  \sectionlabel{Lesson @@LESSONID@@}
  By the end of today, I can\ldots
  \begin{itemize}\setlength{\itemsep}{5pt}
    \item TODO: target 1, naming the formal term.
  \end{itemize}
  \begin{block}{How today works}
    Warm-up $\to$ \textbf{notes together} $\to$ \textbf{one we work as a class} $\to$
    debrief $\to$ \textbf{you start the homework here, alone}.
  \end{block}
\end{frame}

% ── WARM-UP ──────────────────────────────────────────────────────────────────
\begin{frame}[t, plain]
  \ceruleanheader{Warm-Up --- 5 minutes, silent}
  \sectionlabel{spiral review}
  % TODO: the three items in one line each, and the line saying the notes reuse these numbers.
\end{frame}

% ── HOOK — dark frame; leave it UNRESOLVED; section 2's second half settles it ──
{
\setbeamercolor{background canvas}{bg=cerulean}
\begin{frame}[plain]
  \vspace{1.3cm}
  \begin{center}
    {\color{frostmid}\bfseries\small TODO SETUP LINE}\\[0.8cm]
    {\color{white}\bfseries\Large TODO the claim.}\\[0.9cm]
    {\color{goldacc}\bfseries\large TODO the question --- vote.}\\[0.5cm]
    {\color{frostmid}\small We settle this at the end of section 2.}
  \end{center}
\end{frame}
}

% ── I DO — direct instruction begins ─────────────────────────────────────────
{
\setbeamercolor{background canvas}{bg=cerulean}
\begin{frame}[plain]
  \vspace{1.8cm}\hspace{0.8cm}%
  \begin{minipage}{0.86\textwidth}
    {\color{goldacc}\bfseries\small GUIDED NOTES \ \textbullet\ \ I DO}\\[0.7cm]
    {\color{white}\bfseries\Large Open to your Guided Notes page. Fill each blank as we
    name it --- not before.}\\[0.9cm]
    {\color{frostmid}\small TODO terms go in the vocabulary box today. Write each one the
    moment we use it.}
  \end{minipage}
\end{frame}
}

% ── NOTES SECTION FRAMES — two or three per section; these ARE the board ─────
\begin{frame}[t, plain]
  \ceruleanheader{TODO section 1, first move}
  \sectionlabel{notes section 1 $\cdot$ TODO}
  % TODO: the definitions and the worked example exactly as they go on the board.
\end{frame}

\begin{frame}[t, plain]
  \ceruleanheader{TODO section 2, second move --- the crux}
  \sectionlabel[redacc]{notes section 2 $\cdot$ the whole point of today}
  % TODO: the computation, the re-vote, and the rule in a block, with the term in \redink{}.
\end{frame}

% ── WE DO — a LIVE surface: task and data only, no answers ───────────────────
\begin{frame}[t, plain]
  \ceruleanheader{Guided Practice: TODO title}
  \sectionlabel[goldacc]{We do $\cdot$ you hold the pen}
  % TODO: the context and its data, then the lettered parts as questions.
  \begin{block}{The question behind the question}
    TODO: the trap, named.
  \end{block}
\end{frame}

% ── DEBRIEF ──────────────────────────────────────────────────────────────────
\begin{frame}[t, plain]
  \ceruleanheader{Debrief: TODO}
  \sectionlabel{10 minutes $\cdot$ pens down, papers up --- we say these aloud}
  % TODO: the two or three things that go back on the board, then the cold check in a block.
\end{frame}

% ── YOU DO — the homework is the individual practice, started in class ───────
\begin{frame}[t, plain]
  \ceruleanheader{You do: start the homework}
  \sectionlabel[goldacc]{Last 10 minutes $\cdot$ on your own $\cdot$ finish it at home}
  % TODO: the context, the item themes (never the answers).
  \begin{block}{Silent and alone --- I am circulating. TODO form (packet pages or a named Desmos activity), points, due date.}
    Start with TODO items. Item TODO is the one I am reading over your shoulder.
  \end{block}
\end{frame}

% ── CLOSE ────────────────────────────────────────────────────────────────────
{
\setbeamercolor{background canvas}{bg=cerulean}
\begin{frame}[plain]
  \vspace{1.5cm}\hspace{0.8cm}%
  \begin{minipage}{0.86\textwidth}
    {\color{goldacc}\bfseries\small BEFORE YOU GO}\\[0.7cm]
    {\color{white}\bfseries\Large TODO: what changed today, in two sentences.}\\[0.9cm]
    {\color{frostmid}\small Next: Lesson TODO --- TODO one-line preview.}
  \end{minipage}
\end{frame}
}

\end{document}
