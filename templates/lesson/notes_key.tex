%! @@TITLE@@ — Unit @@UNITINT@@, Lesson @@LESSONID@@ — Guided Notes & Practice (Answer Key)
% MIRROR of ../notes/main.tex. Copy the blank VERBATIM, then change exactly these:
%   \usepackage{@@PREFIX@@-boxes}      ->  \usepackage{@@PREFIX@@-key}   (pulls in -boxes; never load both)
%   the \pageheader second argument    ->  Guided Notes \& Practice --- Answer Key
%   \vterm{Term}                       ->  \vtermans{Term}{definition}  (both defined in the preamble)
%   \blank{W}                          ->  \ans{short answer}           (no wider than the blank)
%   \pcell{n}{statement}{H}{}          ->  \pcell{n}{statement}{H}{answer}  (answer shorter than H)
%   \writespace{H}{} / \labelbox{W}{}  ->  the same with the answer / label as the second argument
% Every \begin{work} block, every height, every width, every sub-row stays BYTE-IDENTICAL.
% \ans{} and the \pcell answer are TEXT MODE: wrap \sqrt/\dfrac/\hat/\bar in $...$.
% NO teachernote here — teacher prose goes in the lesson plan.
% Prove it: diff the two files and only the lines above may differ; then the page counts match.
\documentclass[12pt]{article}
\usepackage{@@PREFIX@@-article}
\usepackage{@@PREFIX@@-key}   % pulls in -boxes; do NOT also load -boxes

\begin{document}

\pageheader{Unit @@UNITINT@@, Lesson @@LESSONID@@}{Guided Notes \& Practice --- Answer Key}
% NAMESTRIP: no \namedateperiod here — mirrors the blank.

% TODO: paste the blank here and apply the substitutions above. Examples:
%   \termblank{Variable}          ->  \termans{Variable}{a characteristic recorded for every individual.}
%   \blank{2.0cm}                 ->  \ans{countable}
%   \pcell{4}{Rainfall}{0.9cm}{}  ->  \pcell{4}{Rainfall}{0.9cm}{C --- the gauge rounded a real amount}

\end{document}
